export enum typeEnum {
	DIRECT = 'direct',
	PRIVATE = 'private',
	PROTECTED = 'protected',
	PUBLIC = 'public',
}

export enum Status {
  online = 'online',
  offline = 'offline',
  playing = 'playing'
}

export enum Role {
  member = 'member',
  invited = 'invited',
  owner = 'owner',
  admin = 'admin',
  default = 'default'
}

export enum GameScheme {
	GENERAL,
	REVERSE,
}

export enum Side {
	NO,
	BOTTOM,
	TOP,
	RIGHT,
	LEFT,
}